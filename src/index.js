const addP2 = (x, y) => x + y;
// const addP3 = (x, y) => x + y;
// const addP4 = (x, y) => x + y;
const add = (x, y) => x + y;
console.log(add(5, 3));

const animalsArray = ['dog', 'cat', 'tiger'];

console.log(animalsArray.indexOf('tiger'));

addP2(3, 2)
